# Spring-Examples
## github commands
```
   git clone https://github.com/manasijbeans/spring-boot-examples.git
   git status
   git add .
   git status
   git commit -m "initial import"
   git push
   git status
   history | grep -i git
 ```
